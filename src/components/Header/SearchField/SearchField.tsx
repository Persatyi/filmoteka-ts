import TextField from "@mui/material/TextField";

const SearchField = () => {
  return (
    <TextField id="standard-basic" label="Search movies" variant="standard" />
  );
};

export default SearchField;
